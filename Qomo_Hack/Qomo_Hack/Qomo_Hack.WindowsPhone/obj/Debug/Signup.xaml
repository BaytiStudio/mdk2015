<Page
    x:Class="Qomo_Hack.Signup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Qomo_Hack"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="LayoutRoot">

        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Panel -->
        <StackPanel Grid.Row="0" Margin="19,0,0,0">
            <TextBlock Text="QOMO" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,12,0,0"/>
            <TextBlock Text="Daftar" Margin="0,-6.5,0,26.5" Style="{ThemeResource HeaderTextBlockStyle}" CharacterSpacing="{ThemeResource PivotHeaderItemCharacterSpacing}"/>
        </StackPanel>

        <!--TODO: Content should be placed within the following grid-->
        <Grid Grid.Row="1" x:Name="ContentRoot" Margin="19,9.5,19,0">
            <ScrollViewer Height="Auto">
                <Grid>
                    <StackPanel>
                        <TextBlock Text="Nama Lengkap" FontSize="20"></TextBlock>
                        <TextBox PlaceholderText="Nama Lengkap" Name="nama_lkp"></TextBox>
                        <TextBlock Text="No. Identitas (KTP/SIM)" FontSize="20"></TextBlock>
                        <TextBox InputScope="Number" PlaceholderText="No. Identitas" Name="id_num"></TextBox>
                        <TextBlock Text="Email" FontSize="20"></TextBlock>
                        <TextBox InputScope="EmailSmtpAddress" PlaceholderText="Alamat Email" Name="email"></TextBox>
                        <TextBlock Text="Kata Sandi" FontSize="20"></TextBlock>
                        <PasswordBox PlaceholderText="Kata Sandi" Name="pass"></PasswordBox>
                        <TextBlock Text="Daftar Sebagai" FontSize="20"></TextBlock>

                        <StackPanel Orientation="Vertical">
                            <RadioButton Content="Produsen" Name="produsen"></RadioButton>
                            <RadioButton Content="Distributor" Name="distributor"></RadioButton>
                            <RadioButton Content="Konsumen" Name="konsumen"></RadioButton>
                        </StackPanel>
                        <Button x:ConnectionId='1' HorizontalAlignment="Center" Content="Daftar"                     ></Button>
                        
                    </StackPanel>
                </Grid>
                
            </ScrollViewer>            
        </Grid>
    </Grid>
</Page>

